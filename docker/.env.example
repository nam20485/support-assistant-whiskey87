# Environment Configuration Template
# Copy this file to .env and fill in your values
# NEVER commit .env files with real secrets to version control

# =============================================================================
# Required: Workflow Configuration
# =============================================================================

# Name of the workflow to execute
# See: https://github.com/nam20485/agent-instructions/tree/main/ai_instruction_modules/ai-workflow-assignments/dynamic-workflows
WORKFLOW_NAME=project-setup-upgraded

# Which AI CLI client to use (claude | opencode)
# If not set, will auto-detect available CLIs (preference: opencode > claude)
WORKFLOW_CLIENT=claude

# =============================================================================
# Required: Authentication
# =============================================================================

# Claude CLI Authentication
# Get your API key from: https://console.anthropic.com/
# Can also be provided via Docker secret: /run/secrets/anthropic_api_key
ANTHROPIC_API_KEY=sk-ant-your-key-here

# OpenCode CLI Authentication (if using OpenCode)
# Get your API key from your OpenAI account
# Can also be provided via Docker secret: /run/secrets/openai_api_key
# OPENAI_API_KEY=sk-your-key-here

# =============================================================================
# Optional: Advanced Configuration
# =============================================================================

# Additional arguments to pass to the CLI
# Example: "--temperature 0.7 --max-tokens 4000"
# EXTRA_PROMPT_ARGS=

# Logging format: json | text
# Default: json (recommended for production)
LOG_FORMAT=json

# Debug mode: true | false
# When true, prints configuration without executing the workflow
# Default: false
DEBUG=false

# =============================================================================
# Docker Secrets (Alternative to environment variables)
# =============================================================================

# Instead of setting environment variables, you can use Docker secrets:
#
# 1. Create secrets:
#    echo "sk-ant-your-key" | docker secret create anthropic_api_key -
#    echo "sk-your-key" | docker secret create openai_api_key -
#
# 2. Run container with secrets:
#    docker run --rm \
#      --secret anthropic_api_key \
#      --secret openai_api_key \
#      -v $(pwd):/workspace \
#      your-image-name
#
# The script will automatically read secrets from /run/secrets/

# =============================================================================
# Example Usage
# =============================================================================

# Basic usage with Claude:
# docker run --rm --env-file .env -v $(pwd):/workspace workflow-agent

# With Docker secrets:
# docker run --rm --secret anthropic_api_key -v $(pwd):/workspace workflow-agent

# Debug mode to test configuration:
# docker run --rm --env-file .env -e DEBUG=true workflow-agent

# Custom workflow:
# docker run --rm --env-file .env -e WORKFLOW_NAME=sample-minimal workflow-agent
